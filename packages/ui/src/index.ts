/**
 * @fileoverview `@sn-editor/ui` public API — minimal shared React primitives.
 * Non-goals: full design system (tokens live in apps/web Tailwind config).
 */

export { Button, type ButtonProps } from './Button';
export { Panel, type PanelProps } from './Panel';
