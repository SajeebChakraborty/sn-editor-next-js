/**
 * @fileoverview VPC module placeholder — wire real networking in a later infra phase.
 * // STUB: phase-1 — keep empty so the root stack stays valid without nested modules
 */

# Expect eventual inputs: name, cidr, azs
# Expect eventual outputs: vpc_id, private_subnet_ids, public_subnet_ids
